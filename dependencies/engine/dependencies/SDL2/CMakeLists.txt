message(STATUS "")
if(${ENABLE_PREBUILT_LIBRARIES})
    message(STATUS "=== Prebuilt SDL2 ===")
    add_subdirectory("${CMAKE_CURRENT_SOURCE_DIR}/prebuilt")
else()
    message(STATUS "=== SDL2 ===")
    add_subdirectory("${CMAKE_CURRENT_SOURCE_DIR}/source")
endif()
